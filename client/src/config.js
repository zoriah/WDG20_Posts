export default {
  postsAPI: import.meta.env.VITE_NOTES_API,
};

//erzeugt ein Objekt das einen Schlüssel mit einem Eintrag mit Namen postAPI hat und der Eintrag bekommt ein Wert
//den Wert holen wir uns aus .env mit Hilfe von VITE
